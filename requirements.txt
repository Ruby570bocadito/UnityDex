# Core dependencies (esenciales)
requests>=2.25.0
beautifulsoup4>=4.9.0
scapy>=2.4.4
paramiko>=2.7.0
colorama>=0.4.4

# Database dependencies (opcionales)
psycopg2-binary>=2.8.6
pysmb>=1.2.7
mysql-connector-python>=8.0.23

# Network scanning dependencies
python-nmap>=0.6.1
pyOpenSSL>=20.0.0
cryptography>=3.4.0
pillow>=8.0.0
matplotlib>=3.3.0
pandas>=1.2.0

# Packet capture dependencies
# Nota: pypcap puede requerir instalación manual en Kali Linux
# sudo apt-get install python3-dev libpcap-dev
# pip install pypcap
pypcap>=1.2.0

# Malware analysis dependencies
python-magic>=0.4.18
# Nota: yara-python puede requerir instalación manual
# sudo apt-get install yara python3-yara
# pip install yara-python
yara-python>=4.0.0
pefile>=2021.5.24
# Nota: ssdeep puede requerir instalación manual
# sudo apt-get install libfuzzy-dev
# pip install ssdeep
ssdeep>=3.1

# Network vulnerability analysis dependencies
vulners>=1.5.0
requests-html>=0.10.0
python-whois>=0.7.3

# IoT security analysis dependencies
ipaddress>=1.0.23
concurrent-log-handler>=0.9.19
# Nota: Los siguientes paquetes pueden requerir instalación manual o configuración adicional
# paho-mqtt para MQTT
paho-mqtt>=1.5.0

# Compatibilidad con Kali Linux
# Si encuentras problemas con algún paquete, intenta instalarlo manualmente con apt
# sudo apt update && sudo apt install python3-scapy python3-nmap